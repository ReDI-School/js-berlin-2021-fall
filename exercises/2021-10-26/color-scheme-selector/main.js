let colorSchemes = {
    dark: {
        color: "#dddddd",
        backgroundColor: "#2E294E",
    },
    light: {
        color: "#333333",
        backgroundColor: "#E8DDB5",
    },
    colorful: {
        color: "#17183B",
        backgroundColor: "tomato"
    }

}

let colorSchemeElement = document.getElementById('color-scheme-name');
/**
 * Complete the code for the function below:
 * 
 * - it should read the value from the input element
 * - use an if...else if...else statement to check the value typed by the user
 * - if the value matches any of the color schemes in the object above, change the
 *      colors of the page accordingly, by setting the "color" and the "background-color"
 * 
 */
function changeColorScheme() {

}